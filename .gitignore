# Terraform
**/.terraform/*
*.tfstate
*.tfstate.*
.terraform.lock.hcl

# OS
.DS_Store

# Python
__pycache__/
*.pyc

# Django
db.sqlite3

# Node
node_modules/
dist/

# Django media uploads
apps/api/media/

# Secrets
.env

# Raw survey data (large, downloaded separately)
pipeline/extraction/data/

# dbt
pipeline/dbt/profiles.yml
pipeline/dbt/target/
pipeline/dbt/dbt_packages/
pipeline/dbt/.user.yml
pipeline/dbt/logs/

# Database dumps (used for server migration, not source code)
full_dump.sql

apps/web/.env
media.tar.gz
apps/web/.env.production
pipeline/extraction/so_survey_historical_extracted.json
pipeline/extraction/survey_2026_data_engineering.csv
# Django collected static files (regenerated by collectstatic)
apps/api/staticfiles/
# Stray backup files
*.bak
tfplan
